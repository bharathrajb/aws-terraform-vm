---
- name: Configure Web Server with Nginx
  hosts: webservers
  become: yes
  gather_facts: no  # Bypasses the problematic python serialization setup phase

  pre_tasks:
    - name: Wait for port 22 to become available
      wait_for:
        host: "{{ inventory_hostname }}"
        port: 22
        delay: 5
        timeout: 180
        state: started
      delegate_to: localhost  # Executes natively on the runner host

  tasks:
    - name: Install Nginx via native shell
      shell: dnf install -y nginx

    - name: Start and enable Nginx service via native shell
      shell: |
        systemctl start nginx
        systemctl enable nginx
